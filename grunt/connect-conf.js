/**
  * This is a web server. when run it serves the root of the project on localhost.
  */

var serverTask = {
    options: {
        port: 8081
    }
};

module.exports = {
    server: serverTask
};
